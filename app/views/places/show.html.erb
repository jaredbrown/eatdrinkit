<h1>
	<b><%= @place['name'] %></b>
</h1>

<p>
	<%= link_to 'Check out', new_review_path(:venue_id => params[:id]) %>
</p>

<h2>Reviews</h2>

<% if @all_reviews %>
	<%= render :partial => 'reviews/review', :collection => @all_reviews %>
<% else %>
	No reviews yet
<% end %>