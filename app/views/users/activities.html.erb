<h2>What People Had Recently</h2>

<% if @reviews.size > 0 %>
	<%= render :partial => 'reviews/review', :collection => @reviews %>
<% else %>
	No reviews yet
<% end %>

<p>
	<%= will_paginate @reviews %>
</p>